# knowledge/examples/ — AI 向けの良質な例（few-shot）

> サンプル構造（テンプレートの実演）。採用時に自プロジェクトの模範例を追加する。

spec・ADR・コードの**模範例**を集約し、AI エージェントの出力品質を底上げする（few-shot 参照）。

## 規約

- 「良い例／悪い例」を対で示すと効果が高い（SHOULD）。
- 本番の個人データ・機密を含めてはならない（MUST NOT）。合成データを用いる（standards/security-standards.md「2.」）。
- 変更クラス: 既定 Class D。
